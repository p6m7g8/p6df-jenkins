#!/usr/bin/env groovy

pipeline {
  agent any

  options {
    disableConcurrentBuilds()
   }

   stages {
     stage('Stage 1') {
       steps {
         sh 'apt list ; dpkg list ; aws --version ; python --version'
       }
     }
   }
}
