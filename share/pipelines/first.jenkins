#!/usr/bin/env groovy

pipeline {
  agent any
  options {
    disableConcurrentBuilds()
    timestamps()
   }
   stages {
     stage('Stage 1') {
       steps {
         sh 'echo Running Stage 1'
       }
     }
   }
}
